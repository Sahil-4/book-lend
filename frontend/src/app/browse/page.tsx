import { BooksList } from "@/components/common";
import { SearchBox } from "@/components/layout";

const Page = () => {
  return (
    <main>
      <SearchBox />
      <BooksList />
    </main>
  );
};

export default Page;
